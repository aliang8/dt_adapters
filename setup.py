from setuptools import setup

setup(
    name="dt_adapters",
    version="0.0.1",
    description="Decision Transformer Adapters",
    packages=["dt_adapters"],
    install_requires=["gym"],  # And any other dependencies required
)
